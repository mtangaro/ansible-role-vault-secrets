---
# defaults file for ansible-role-user-passphrase

# Enable Vault secrets management

# Retrieve secrets and store as fact or env file
# Possible options are:
# - fact
# - envfile
save_secrets_as: "fact"

# Path for envfile
env_file: '.env.secrets'
env_file_path: '/tmp/'
